export type NavLink = {
  label: string;
  href: string;
};

export type SocialLogo = {
  name: string;
};

export type Partner = {
  name: string;
  logoUrl: string;
  description: string;
  specialty: string;
};

export type Feature = {
  title: string;
  description: string;
  icon: string;
};

export type FeatureSplit = {
  eyebrow: string;
  title: string;
  description: string;
  image: string;
};

export type FaqItem = {
  question: string;
  answer: string;
};
